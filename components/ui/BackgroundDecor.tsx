import React from 'react';

/**
 * BackgroundDecor was used for decorative blobs/bubbles in the background.
 * Now returning null as per user request to remove background decor across all pages.
 */
export const BackgroundDecor = () => {
  return null;
};
